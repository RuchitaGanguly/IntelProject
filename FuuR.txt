Fuu
Asg 7
•	In bitvise 
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install nginx
cd /
cd var/www
sudo chmod 777 html

Asg 8
(Deploy project from local machine to GitHub)
•	In git bash
•	If in master then convert to main by 
git branch -m master main
git init
git add .
git status
git config --global  user.email "ruchitaganguly739@gmail.com"
git config --global  user.name "Ruchita Ganguly"
git config --global --get-all  user.email 
git config --global --get-all  user.name
git commit -m "Initial Commit"
git remote add orgin "http://github.com/RuchitaGanguly/AWS_P8.git"
•	If already orgin present then first remove by 
git remote remove origin
git remote -v
git push -u origin main

(Deploy a project from git hub to local host)
•	Open the gitbash through that folder
git clone "http://github.com/RuchitaGanguly/AWS_P8.git"

Asg 9
(Deploy project from GitHub to EC2)
sudo apt-get update
sudo apt-get upgrade
sudo apt-get install nginx
curl -sL https:\\deb.nodesource.com\setup_16.x|sudo -E bash
sudo install npm 
sudo apt install nodejs 
git clone "http://github.com/RuchitaGanguly/AWS_P9.git"
ls
Cd AWS_P9
•	In case required for express
npm install express
node index.js

Asg 10
(Deploy project from GitHub to EC2 by creating new security and user data)
•	In user data
#!/bin/bash
apt-get update
apt-get upgrade
apt-get install -y nginx
systemctl start nginx 
systemctl enable nginx
apt-get install -y git
curl -sl https://deb.nodesource.com/setup_18.x | sudo -E bash -
apt-get install -y nodejs

•	Open the terminal
git clone "http://github.com/RuchitaGanguly/AWS_P9.git"
npm install express
cd AWS_P9
ls
node index.js


Asg 11
•	In user data 
#!/bin/bash
apt-get update
apt-get upgrade
apt-get install nginx
systemctl start nginx
systemctl enable nginx
apt-get install -y git
curl sL https://deb.nodesource.com/setup_18.x | sudo -E bash -
apt-get install -y nodejs
git clone https://github.com/RuchitaGanguly/AWS_ASG9.git
cd AWS_ASG9
npm install
node index.js

•	In bitwise SSH 
nano ass11.sh
#!/bin/bash
while(true)
      echo "Loop Running"
done
(Exit by pressing control X then Y then Enter)
chmod +x ass11.sh
./ass11.sh

Asg 12
•	In user data 
#!/bin/bash
apt-get update
apt-get upgrade
apt-get install nginx
systemctl start nginx
systemctl enable nginx
apt-get install -y git
curl sL https://deb.nodesource.com/setup_18.x | sudo -E bash -
apt-get install -y nodejs
git clone https://github.com/RuchitaGanguly/AWS_ASG9.git
cd AWS_ASG9
npm install
node index.js
•	In Bitwise Ssh 
pwd
cd /
ls
cd nginx
•	If nginx not install then use sudo apt install nginx
ls
cd sites-available
ls
sudo nano default
•	COde:
{
proxy_pass http://localhost:4000;
proxy_http_version 1.1;
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection 'Upgrade';
proxy_set_header Host $host;
proxy_cache_bypass $http_upgrade;
}
cd ..
cd ..
cd ..
sudo systemctl restart nginx
